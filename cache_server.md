Hi, I'd like you to tell me about something I was just reading about called document-driven databases as a new, very popular concept, as opposed to relational databases. And it was particularly mentioning JSON fields, and I actually just encountered that recently with my SQL database project where I was working with a field that was set to JSON and trying to figure out if I wanted to keep that as JSON or move it just to be a text field and wondering what the benefits of the JSON field are. Now, I noticed the reason I caught it was because the JSON field wasn't honoring, I was trying to sort the key, the keys, and it wasn't updating because it saw it as the same JSON effectively. And so that's why I changed it to text so I could sort those keys, but then I'm wondering maybe I should go back to JSON, or what are the benefits of it? Can you tell me about that? And also, can you explain more about document databases, please? Thank you.
You said:
Tell me about like a hybrid approach where you're building a document style database off of SQL such that like you've got a basic set of info in one table, but then that table has then a JSON field attached to it as well that can hold whatever extra other stuff you want. So if there's like derived classes that they're going to store more data than just the basic class, then they have a place to put it. But still using an existing relational database structure, just trying to like redesign it so that it can mimic a document system and not have to join tables together.
You said:
So one thing that's occurring to me reading what you're saying here is that, let's say I want to just modify one key value pair, but there was 30 defined in there. Do I need to read the whole thing in and then programmatically, surgically change the one part, and then set the whole new thing back in? Or does the MySQL JSON field type allow you to set just one thing in there? Or delete just one thing in there, but leave all the rest? Or do little tricks like that?
You said:
No, I wasn't avoiding them for any reason other than I didn't know about them until about an hour ago. So it's really changing the way I might redo all the architecture of this system, and it could really simplify getting rid of so many tables, which would be fantastic. And it also could potentially give me a more plausible path forward to say that the database could expand well. I've been kind of wondering about how this system I'm building will grow if I get a bunch of users on it, because right now it works fine just me using it, testing it, you know, but if I got dozens or hundreds of people hitting the site simultaneously all wanting to do, you know, interactive operations, I don't think it would be thousands of people ever. I mean, maybe, but it's not going to become like Amazon. It's just it's a small niche of people I think would use it. But I would hate to discover that all of my infrastructure was only scalable up to like three users concurrently before you start noticing lag, and then trying to do standard ways to expand that. If I didn't plan better, if those failed, that would be a bummer.
You said:
Okay, that's great info. And actually, I wasn't worried about JSON slowing things down, to tell you the truth. Everything you pointed out about how it would make things handier, that all sounds good to me too. I was thinking it would speed things up, just not having to do the joins. And especially if there was, for example, I'm displaying a page that has 100 different types of items on it, which would otherwise have required joins to 100 different types of tables. That could all just stay within one table. Makes it seem faster to me. But it's kind of a separate question about me thinking about, how do I scale this site and system up? And what would become bottlenecks on things? And I'm imagining like, well, I could always separate the MySQL server and put that on a different box than the NGINX server. And actually, I'm doing NGINX with a Flask app. It's like a Flask stack is what I'm doing. And I have NGINX routes to different Flasks, daemons that are running, which then run individual sub-process calls to a gateway, Python script, which is like a one-shot affair. And that thing goes into my whole system of Python code with lots of stuff going on. And in there is where pyMySQL is going to be making calls to the MySQL server. And I'm imagining that my Flask setup, well, geez, there could just be different, there could be multiple ports that listen. And I could have multiple Flask apps running on more cores or whatever. There's got to be a bunch of different ways NGINX could cleverly route and distribute that to a Flask, distributed Flask load. I'm just guessing with my limited knowledge of how Flask works. But then if they're all pointing back to the same MySQL server, that's in my mind where the bottleneck would come with my system. What do you think about that in a general sense?
You said:
Okay, here are three I think would be the most common sort of read-only access patterns. One would be take a page specified by its ID and show it plus all of the pages that are its direct child, like meaning their parent field is set to this ID. Both of those are indexed columns. Second would be digging down the hierarchy tree further, like maybe even digging all the way down to the bottom of any hierarchy tree off a page and returning a whole group of pages, all that match the hierarchy tree. And then the third option would be a list of individual pages that are unrelated to each other but might have some degree of that same hierarchy tree walking, always specified by their ID or by a name field that they match to, text name field that would be unique.
You said:
Okay, this is good info. My users might actually be creating some tree structures that become vast. So here's the application. We're talking about creating a database of every puzzle design ever in the world. And so collectors can use it to manage their collections and link to the master records that will be stored in sort of this known system. But then also designers and manufacturers can also link in there. So an individual master level, top level thing for the Rubik's Cube, geez, that might have a million nodes. Everybody would want to put their freaking Rubik's Cube pictures up there or whatever. But I wouldn't necessarily want to walk that entire tree all the way down. So to refine the question, what if we're only going to not necessarily walk all the way to the bottom? Because you could do that programmatically with some sort of guardrails to not have it walk too deep and take forever. But what if you wanted to walk just two or three tiers deep? Does that change the way that they get structured?
You said:
Okay, so one facet of this system is that it has a sort of wiki-like syntax where it can, in a text field of each page row, it parses with a grammar engine looking for the syntax, and then it will find matching other pages based on either their ID or their name, and then it will, it has a whole cool decorator system on top of that that can run other stuff, and so anyways, when the text processor runs, it takes the raw text, which is stored in the field, and it converts it to parsed text, which is a somewhat complicated process, which can involve lots of database queries, especially if it gets involved with some deep stuff. Anyways, so I've noticed in the past that that's where bottleneck happens, especially if I'm trying to get a tree of even a couple grandchildren deep or whatever, and I want to get that parsed text from every player involved, it's having to do all this parsing. So I've played around in years past with ways of like buffering that and storing it. Hang on, let me start another voice text, but this will be round one of two, and then I'm going to tell you more.
You said:
Okay, thanks. Yeah, and I found I have to do break in the small parts for the voice-to-text, so it With the way that it would cache basically the computation of all of the Text parsing strings would turn that into again just a simple page document style flat access and so all of your oh you know Algorithm complexity analysis that would come back to what you actually said if you don't have to do any processing but then when you update the Page text it would have to reprocess it recache it fine. No problem And then even if when you display a page, it's dynamically generating and based on itself. It's children. It's grandchildren It's grabbing all of their Cached page text it should still be able to display all that quickly And then go ahead and pause and I'll tell you to round three of the voice-to-text coming up next
You said:
Yep, and so I have a separate table called links and and that has just the relational links between the id of the page that has its text field with a wiki grammar defined linking somewhere else and then it tracks what the actual text of that link was and then it tracks what page id it links to so that for every entry that makes a link so if a text field has a dozen different wiki links in it updating that page's text field will wipe out all 10 of those relational row entries in that links table and then add 10 more new ones that properly handle all of the parsing of the wiki syntax and so if you change the name of a page it uses that so it can go look and see in the links table any page that linked into it and was using the name as the way it linked rather than the id number you need to go into each of those linking pages parse through their text fields and update the text so that it is referencing the new lay the new name to link to not the old name otherwise everything falls out of sync and the system currently works that way it's just a little bit of a question mark of how do you notate on these pages that they're stale now i'm gonna hit send and then we'll do one more text after this so stay tuned
You said:
So my old system, I think, when I was playing with that in the past, had like a stale references flag or something and a last modified flag. I'm not sure. It involved a little bit of extra bullshittery. I have found in the past that this kind of stuff, having to catch these results and then knowing when it was stale and then knowing when to have to recalculate it so that a page would, like let's say you change the name of the Rubik's Cube and now it has to update a million different pages text to do that new thing. That's one operation, but then all those million pages are going to probably want to regenerate their page text. So it gets to be a little bit of a can of worms and I wanted to talk to you about that in terms of the scalability and what might be a smart way to implement some kind of caching system there.
You said:
Okay, so I like the idea of having a cached pages table as a separate table from the main table, and in fact, that could even grow into a separate database server if it needed to for read-only stuff. And then if a page gets invalidated because its references are stale, it could just get its row deleted from the cached database table or the cached database server or whatever, and then when any read request for that page comes up, it looks in the cached server first, and if it's not found there, then it will go to the main server and trigger a rebuild, or maybe the cached server is just smart enough to trigger that rebuild on its own. And if there was a queue of updates, the million name changes that have to propagate, sure, that could be done lazily such that if a page was listed as needing to be updated and then recomputed, it wouldn't happen until somebody actually requested that page for any reason, and then a daemon in the background could chip away at any huge things. And likewise for recalculating the processed text after it gets updated, that could be done lazily to jump something to the head of the queue of recalculation perhaps. What do you think about that? Is that a common pattern?
You said:
So what I'm imagining here is that the cache system, which could be a separate box running with a no DB rather than relational DB, that could still handle all those, you know, couple tier deep grandchildren kind of tree traversals and stuff, and just very simple cache server system. But then we could have much more complex business logic going on in, like, the MySQL database, which has, like, the links table to handle all that text processing engine stuff and any other systems going on, which would affect the generation of the cached pages, such that for all of the basic info that anybody would be wanting in terms of getting info, assuming the cache was updated, it could just get it right there. And if they're fine with stale info, they can get it right away.  what do you think about that?
You said:
So I think the way I would want to approach this is to set it up so that it has all of this architecture built in right from the get-go, but designed to still run on my junky little Mac mini Ubuntu box. And so it would be the same MySQL server, either having two different databases that it's serving, or I'm not sure what exactly, but some, you know, and configuration files that point to which server and it will go to get its cache versus which server it will go to get its whatever. And so, you know, as long as when, if when the system grows to a growth point where we're now separating out the NGINX and Flask part of it from the MySQL part, and then if we then grow further and we separate the MySQL part into maybe some separate NoDB system as three now boxes running to try to keep up with the load of the system, that third step to, you know, shear off the cache server at that point, it wouldn't affect any of our code. It would just be configuring one file about where to point to that server and then set up that server as a separate thing, right?
You said:
Okay, so my Python code that I'm working with here, I've got a decorator system for all my database connections where I can decorate it with db.read or db.write, and the db.write is a whole transaction thing with some other stuff built into it too, but the db.read is pretty simple, and so I'm imagining that it'd be logical to tie the reads to the cache database and the writes to the main database, even if it was going to be selecting info rather than updating or creating. Still, if it has the right decorator on it, it would be getting its info from the actual system, not just from the cache system, and so that the read decorators, or either that, or we have a third decorator we create, which would be db.cache, and so you would decorate it, well, I'm saying this out loud, but then I don't want to double decorate it with db.cache and then db.read to get what it needs. So realistically, I think that first thing I was saying is the proper way to go about it, where each of the db.read and db.write point to their respective separate sources. What do you think about that?
You said:
Okay, let's talk a little bit more about the invalidation logic, what I say, stale references. We change the name of Rubik's Cube to something else, and it's going to need to modify a million other page texts, which then have a million recomputes back to the cache server. And so, the first step would be to, I mean, jeez, you're still going to have to deal with a million at some point, but let's just say you're going to deal with that first million, and you're going to queue up a million things to work on. Or maybe you queue up just the query itself and say, we have to do this update. And that goes into some sort of engine now where there's a daemon running on it, and it runs that query as accounts just to find out how many still need to be done. And if there's more that need to be done, then it grabs one off the list and does it. And that way, changing the name of the Rubik's Cube does not involve any huge thing other than adding that name update task to the list of some daemon activities, which would be its own database table in the main database. And then we have our Flask apps that are running, and they're continually running. And so, when they're idle, they could just go check that table and go do something. And there could even be some prioritization involved where if they're getting a request for something that's marked as invalid, they can quickly jump that to the top of the list and do it before processing the request so it's apparent from the user.
You said:
So then in a system like I'm describing here, a stale reference would never want to actually proactively delete from the cache database. You would always want to have your most recently computed cache available for serving for people for any read request. I'm only imagining we would want to force recomputes when people want to do like more updates like write requests or something, you know, where they're, let's say they're an admin user and they've got an editor form open and they're changing the Wikipedia, so to speak, you know, in which case they need to make sure that they get a fresh page load of what that page actually looks like with any pending updates already done to it. And so that would be a circumstance where it would want to bump it to the top of the priority queue and any dependencies so that that can return right away because it's blocking some admin user from being able to even get their initial page load. Whereas any other, like pretty much any other kind of request that's not of some kind of editing level of CRUD would just be happy to serve the stale references fine and the daemon can chug through all that updates eventually, you know, and then when the update happens it just overwrites the cache table at that point. Does that seem like a normal standard plan? Good idea, yeah?
You said:
So if we come back to the idea of breaking apart db.read versus db.write decorators, and I guess a question of like, what gets locked up in the transaction of the database? So my write decorator is structured such that it creates a whole transaction that can be rolled back or committed at the end at any point, you know, and does that then lock up the database so that no other users can even get a read off the same tables until the write transaction is done? Is that how that works? So let's say I want to edit a file and it's got a bunch of, not a file, it's a field page in the database, it's got a bunch of stuff that it has to change about it, and so it's going to be a 40, you know, 0.45 second long transaction, half a second, for it to make all those writes to the system, and is that going to prevent for half a second any other user from being able to also get their own information from a db.write decorator that they're specifying off the same table?
You said:
Okay, gotcha. And so let's say I've decorated a function with dbWrite, and let's say I am passing that connection that now, because the decorator inserts a connection variable into the first argument of whatever function it decorates is how it works, and then those functions can use that connection variable. And they can, when we're in a transaction mode, they will take that connection from the first one that got decorated, they'll pass it down the chain to any further pages they call by using a different getpage command that has the getpage for connections rather than the getpage for decorators, which would be the initial call. And so that's how I structure all these transactions together. But sometimes, you know, they're just doing select calls, they're just reading data. And so that's still fine too, right? That's all maybe part of the same transaction. They don't need a different decorator for them to be able to run a select query on that database they're hooked to with that connection. They just, they can run it like normal. And it's, it just all still falls into that same commit envelope, like what you're talking about in the end. So there would never be any need to give them another decorator besides that one, right?
You said:
Okay, and the DB redecorator could technically still have a connection that it has to the other server too, right? Like when it passes its connection object, that connection object, that single argument that it inserts, could have multiple parts to it, right? And so that way if, let's say we're saying I want to move my page from here to there, or I want to copy images from some other page to into this page, I'm going to want to navigate my system and have an interface that kind of walks through it, and that's going to want to access the cache server to propagate all that. In order to pick the location, or to pick the images that I want to do something from, and then that would feed into the create, you know, of the real database doing the CRUD work. But it would still be getting some info from the cache database for some of it, and so rather than have to modify my functions to take two connection arguments being inserted by now the DBRite, could we have it where there's the DBRite has both connections built into it?
You said:
So then we could theoretically have the db-read decorator do the same thing and have access to both databases so that it could simultaneously grab the quick cache version of the page as a default action, but then two, it can find out if there's any stale references that need to be determined, and if so, then it can bump that up the priority queue or something so that at least the next person accessing it doesn't have to wait. And that way it can be a user request-driven kind of prioritization thing rather than having to be smart about it. And then theoretically even there could be a fetch API kind of thing that runs in the background. And once that recalculation that got bumped to the top of the queue completes, it can behind-the-scenes slip in whatever changed.